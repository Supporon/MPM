# AGENTS.md

## 变更日志机制（change_log）

本仓库要求：**每次修改代码后，必须在仓库根目录的 `change_log.md` 追加一条变更记录**，格式对齐实验代码 `lachlan_dataset_audit/WORKLOG.md`。

### 触发时机

以下任一动作完成后，都要追加记录：

- 新增、修改、删除任何代码文件（`src/`、`scripts/`、`run.py`、`baseline_tools.py`、`tests/`、`configs/` 等）。
- 完成一次修复、重构、功能新增或配置变更。
- 一次交互可能包含多处修改，可合并为一条记录；但不得漏记。

### 格式

每条记录由「时间戳节标题 + 无序列表正文」组成，追加到文件末尾（时间递增，最新在最后）：

```markdown
## YYYY-MM-DDTHH:MM:SSZ
- 变更内容 1（改了哪个文件/组件）。
- 变更内容 2（为什么改、怎么改）。
- 验证结果（测试/运行统计/产物；如未验证则写「未运行验证」）。
```

### 记录要求

- 时间戳使用 UTC 的 ISO 8601 格式，秒级精度，以 `Z` 结尾（例如 `2026-09-04T09:47:00Z`）。
- 正文用 `- ` 无序列表，语句简洁、可追溯，覆盖「改了什么 / 为什么改 / 结果如何」。
- 记录追加到文件末尾，保持与 `WORKLOG.md` 一致的时间递增顺序。
